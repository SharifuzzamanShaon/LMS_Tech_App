/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com','lh3.googleusercontent.com','res.cloudinary.com'], // Add the external image domain here
      },
};

export default nextConfig;
